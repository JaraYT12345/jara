// Función para mostrar una alerta
function mostrarAlerta(demostracion) {
    alert('Has hecho clic en ' + demostracion);
}

// Función para cambiar el color de fondo del cuadro
function cambiarColor(elemento, color) {
    elemento.style.backgroundColor = color;
}